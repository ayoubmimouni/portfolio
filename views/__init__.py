"""Page bodies.

Each module (except `auth` and `_shared`) is a Streamlit page script declared in
`app.py` via `st.Page`, so only the active page executes on a run — natural code
splitting for the app.
"""
