"""UI layer: design tokens, global styles, icons, components and charts.

This package owns everything visual. It has no knowledge of the portfolio
business logic — views compose `ui` primitives with data coming from
`services`, which in turn talks to the FastAPI backend and market data.
"""
